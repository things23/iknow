<% selector = "#comments-#{@commentable.class.to_s.downcase}-#{@commentable.id}" %>

$("<%= selector %>").html("<%= j render 'comments/comments', commentable: @commentable, comments: @commentable.comments %>" );